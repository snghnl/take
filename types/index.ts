export interface HLData {
  id: string;
  text: string;
  bg: string;
  colorName: string;
}

export interface ArchiveItem {
  id: string;
  url: string;
  title: string;
  textContent: string;
  date: string;
}
